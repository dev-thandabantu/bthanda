import DamageReportPage from "@/components/DamageReportPage";

export const metadata = {
  title: "Damage Report — Mlungisi Nhlapo",
  robots: "noindex, nofollow",
};

export default function Page() {
  return <DamageReportPage />;
}
